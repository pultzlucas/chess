export default class PieceCase {
    x;
    y;
    color;
    piece;
    constructor(x, y, color) {
        this.x = x;
        this.y = y;
        this.color = color;
        this.piece = null;
    }
}
