{
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d ./",
    "build": "tsc"
  },
  "dependencies": {
    "@types/firebase": "^3.2.1",
    "firebase": "^9.17.2",
    "module-alias": "^2.2.2",
    "typescript": "^4.9.5"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
